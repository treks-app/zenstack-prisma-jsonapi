{
  "name": "zenstack-rest-gen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsx --watch server.ts",
    "gen": "npx zenstack generate && npx prisma generate && npx prisma db push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "@zenstackhq/runtime": "^2.14.0",
    "@zenstackhq/server": "^2.14.0",
    "fastify": "^5.3.0",
    "fastify-plugin": "^5.0.1"
  },
  "devDependencies": {
    "prisma": "^6.6.0",
    "tsx": "^4.19.3",
    "zenstack": "^2.14.0"
  }
}
